# Copie para .env.local se precisar sobrescrever.

# Alvo do proxy /api durante `npm run dev`
VITE_API_TARGET=http://localhost:8080

# Token de admin de dev (Etapa 2 troca por auth real)
VITE_ADMIN_TOKEN=dev-admin-token
